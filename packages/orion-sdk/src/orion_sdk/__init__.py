"""orion-sdk — Agent runtime SDK。

Conversation loop、tools、MCP、sandbox、memory 等核心 module。依賴 orion-model
提供的 LLM provider 抽象。

用法:
  from orion_sdk.core.conversation import Conversation
  from orion_sdk.state import AgentContext
"""
