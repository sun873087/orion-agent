# Copy to .env and fill in your keys.
# Only set the providers you intend to use.

# Anthropic API key — https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI API key — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxx

# Optional: override base URLs (e.g. proxy / Bedrock / Together AI)
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: max output tokens per assistant turn (default 16384).
# Increase if long code answers get cut off (stop_reason=max_tokens).
# Anthropic Sonnet/Opus 4.x 支援到 64000;只在模型真的吐這麼多時才計費。
# ORION_MAX_TOKENS_PER_TURN=32768
